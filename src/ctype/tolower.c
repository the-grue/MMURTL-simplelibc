/************************************************
* Make char lower case
*************************************************/
int tolower(int chr)
{
	if ((chr >= 'A') && (chr <= 'Z'))
		return (chr + 0x20);
}
